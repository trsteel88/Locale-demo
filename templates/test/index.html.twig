{% extends 'base.html.twig' %}

{% block body %}
    {{ render(controller('App\\Controller\\TestController::subRequestAction')) }}

    {{ form_start(form) }}
    {{ form_errors(form) }}

    {{ form_row(form.name) }}

    {{ form_end(form) }}
{% endblock body %}
